<?php
/**
 * File generated by the Sydney_Admin_Generator v2.0
 */

/**
 * Mapping of the FilmetadataFiltype table in an object
 * @package Admindb
 * @subpackage ModelGenerated
 */
class FilmetadataFiltype extends FilmetadataFiltypeOp
{
    protected $_schema = 'sydney';
    protected $_name = 'filmetadata_filtype';
    protected $_referenceMap = array(
        'Filmetadata' => array(
            'columns'       => 'filmetadata_id',
            'refTableClass' => 'Filmetadata',
            'refColumns'    => 'id'
        ),
        'Filtype'     => array(
            'columns'       => 'filtype_id',
            'refTableClass' => 'Filtype',
            'refColumns'    => 'id'
        ),
    );
    protected $_primary = 'id';

    public $fieldsNames = array(
        'filmetadata_id', // bigint()
        'filtype_id', // bigint()
        'id', // bigint()
    );

    protected $_metadata = array(
        'filmetadata_id' => array(
            'SCHEMA_NAME'      => null,
            'TABLE_NAME'       => "filmetadata_filtype",
            'COLUMN_NAME'      => "filmetadata_id",
            'COLUMN_POSITION'  => 1,
            'DATA_TYPE'        => "bigint",
            'DEFAULT'          => null,
            'NULLABLE'         => false,
            'LENGTH'           => null,
            'SCALE'            => null,
            'PRECISION'        => null,
            'UNSIGNED'         => null,
            'PRIMARY'          => false,
            'PRIMARY_POSITION' => null,
            'IDENTITY'         => false,
        ),
        'filtype_id'     => array(
            'SCHEMA_NAME'      => null,
            'TABLE_NAME'       => "filmetadata_filtype",
            'COLUMN_NAME'      => "filtype_id",
            'COLUMN_POSITION'  => 2,
            'DATA_TYPE'        => "bigint",
            'DEFAULT'          => null,
            'NULLABLE'         => false,
            'LENGTH'           => null,
            'SCALE'            => null,
            'PRECISION'        => null,
            'UNSIGNED'         => null,
            'PRIMARY'          => false,
            'PRIMARY_POSITION' => null,
            'IDENTITY'         => false,
        ),
        'id'             => array(
            'SCHEMA_NAME'      => null,
            'TABLE_NAME'       => "filmetadata_filtype",
            'COLUMN_NAME'      => "id",
            'COLUMN_POSITION'  => 3,
            'DATA_TYPE'        => "bigint",
            'DEFAULT'          => null,
            'NULLABLE'         => false,
            'LENGTH'           => null,
            'SCALE'            => null,
            'PRECISION'        => null,
            'UNSIGNED'         => null,
            'PRIMARY'          => true,
            'PRIMARY_POSITION' => 1,
            'IDENTITY'         => true,
        ),
    );

    /**
     * Returns the list of IDs linked to a "Filmetadata"
     *
     * @param Int $id ID of the filtype
     * @return Array list of IDs
     */
    public function getFilmetadataLinkedTo($id)
    {
        if (preg_match("/[0-9]{1,9}/", $id)) {
            $toreturn = array();
            $rows = $this->fetchAll($this->select()->where('filtype_id = ?', $id));
            foreach ($rows->toArray() as $v) {
                $toreturn[] = $v['filmetadata_id'];
            }

            return $toreturn;
        } else {
            return array();
        }
    }

    /**
     * Sets the data linked to an id in the "Filmetadata" table
     *
     * @param Int $id ID of the filtype
     * @param Array list of IDs
     * @return Boolean true if success and false if there were an error
     */
    public function setFilmetadataLinkedTo($id, array $data)
    {
        if (preg_match("/[0-9]{1,9}/", $id) && is_array($data)) {
            $where = $this->getAdapter()->quoteInto('filmetadata_id = ?', $id);
            $this->delete($where);
            foreach ($data as $v) {
                $this->insert(array(
                    'filmetadata_id' => $id,
                    'filtype_id'     => $v
                ));
            }
        } else {
            return false;
        }
    }

    /**
     * Returns the list of IDs linked to a "Filtype"
     *
     * @param Int $id ID of the filmetadata
     * @return Array list of IDs
     */
    public function getFiltypeLinkedTo($id)
    {
        if (preg_match("/[0-9]{1,9}/", $id)) {
            $toreturn = array();
            $rows = $this->fetchAll($this->select()->where('filmetadata_id = ?', $id));
            foreach ($rows->toArray() as $v) {
                $toreturn[] = $v['filtype_id'];
            }

            return $toreturn;
        } else {
            return array();
        }
    }

    /**
     * Sets the data linked to an id in the "Filtype" table
     *
     * @param Int $id ID of the filmetadata
     * @param Array list of IDs
     * @return Boolean true if success and false if there were an error
     */
    public function setFiltypeLinkedTo($id, array $data)
    {
        if (preg_match("/[0-9]{1,9}/", $id) && is_array($data)) {
            $where = $this->getAdapter()->quoteInto('filtype_id = ?', $id);
            $this->delete($where);
            foreach ($data as $v) {
                $this->insert(array(
                    'filtype_id'     => $id,
                    'filmetadata_id' => $v
                ));
            }
        } else {
            return false;
        }
    }

    public function __construct($id = 0)
    {
        $this->_schema = Sydney_Tools_Sydneyglobals::getConf()->db->params->dbname;
        parent::__construct($id);
    }

}
